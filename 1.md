apt install nodejs
yt-dlp -U 需要每天更新，不然接口会变化
yt-dlp --js-runtimes node <url>

yt-dlp --bind-address <IP地址> <URL>
yt-dlp --proxy "socks5://[2001:db8:1234::2]:1080" <URL>

ip -6 route add default via <gateway> dev eth0 table 101
ip -6 rule add from 2001:db8:1234::1 table 101

aria2 + IPv6 轮询
